import SwiftUI

@main
struct CadenceTrackerApp: App {
    var body: some Scene {
        WindowGroup {
            // ContentView() // Commented out for debugging
            Text("Debug Mode")
                .foregroundColor(.white)
        }
    }
}
